#!/usr/bin/env bash
# omnix-status — OMNIX front-door runner wrapper

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

python3 "$PROJECT_ROOT/src/openjarvis/omnix_frontdoor.py" "$@"
